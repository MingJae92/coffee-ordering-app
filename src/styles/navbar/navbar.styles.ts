import { SxProps, Theme } from "@mui/material";

export const navStack: SxProps<Theme> = {
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
};

export const logoStyle: SxProps<Theme> = {
  fontWeight: "bold",
};
